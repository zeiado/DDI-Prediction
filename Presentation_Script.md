# عرض مشروع: نظام ذكي للتنبؤ بالتفاعلات بين الأدوية  
*Script جاهز بالعربي لطالب ثانوي – بصيغة Markdown*

> **ملاحظة للطالب:**  
> مش لازم تقول الكلام حرفيًا، المهم الفكرة. اقرأ السكريبت كويس، افهمه، وبعدين عدّله بطريقتك لو حابب.

---

## 1. المقدمة (حوالي 30–45 ثانية)

السلام عليكم،  
أنا اسمي **…(اسمك)**، وطالب في الصف **…**.

مشروعي اسمه:  
**"نظام ذكي للتنبؤ بالتفاعلات بين الأدوية"**  
أو بالإنجليزي: **Drug-Drug Interaction Prediction**.

فكرة المشروع باختصار:  
في مرضى كتير بياخدوا أكتر من دواء في نفس الوقت، وده ممكن يسبب **تفاعل خطير** بينهم.  
المشروع بتاعي بيستخدم **الذكاء الاصطناعي** عشان يتنبّأ هل في تفاعل خطير بين دوائين ولا لأ، ويساعد الطبيب أو الصيدلي ياخد قرار آمن.

---

## 2. المشكلة اللي بحلّها (حوالي دقيقة)

المشكلة الأساسية هي:

- أحيانًا المريض بياخد:
  - دواء من دكتور القلب  
  - ودواء تاني من دكتور تاني  
  - وفيه أدوية تالتة من الصيدلية  
- الجمع بين الأدوية دي ممكن يعمل:
  - تفاعل شديد  
  - يقلل تأثير دواء  
  - أو يزود تركيز دواء تاني في الدم بشكل خطير

الطبيب والصيدلي مش دايمًا يقدروا يحفظوا **كل** التفاعلات بين آلاف الأدوية.

من هنا جات **فكرة المشروع**:  
أعمل نظام ذكي يقرأ بيانات عن الأدوية ويتعلم منها، وبعد كده يساعدنا نعرف:

- هل في تفاعل بين دوائين؟
- ولو في، ده:
  - **شديد (Severe)**  
  - **متوسط (Moderate)**  
  - **أو مفيش تفاعل مهم (None)**

---

## 3. فكرة الحل بشكل بسيط (حوالي دقيقة)

الحل عندي عبارة عن 3 أجزاء رئيسية:

1. **تجهيز البيانات (Data Preprocessing)**  
2. **تدريب نموذج ذكاء اصطناعي (Model Training)**  
3. **تطبيق عملي يرد على الأسئلة (API / Prediction)**  

هشرح كل جزء ببساطة.

---

## 4. جزء تجهيز البيانات (Data Preprocessing)

الملف المسؤول عن الجزء ده اسمه تقريبًا: `data_preprocessing.py`.

إيه اللي بيحصل فيه؟

- عندي ملفات بيانات بصيغة CSV فيها:
  - أسماء الأدوية
  - كود كيميائي لكل دواء اسمه **SMILES**
  - تفاعلات حصلت بين دوائين ووصف التفاعل

أنا بعمل الآتي:

1. أقرأ ملفات الأدوية والتفاعلات.
2. أحوّل صيغة **SMILES** لتمثيل رقمي اسمه **Molecular Fingerprint**:
   - نقدر نعتبره **بصمة رقمية للدواء**.
3. لكل زوج أدوية (Drug 1, Drug 2):
   - أحوّل الاثنين لـ fingerprint
   - أدمجهم في متجه واحد من الأرقام.
4. أصنّف التفاعل لواحدة من 3 مستويات:
   - `Severe` (شديد)  
   - `Moderate` (متوسط)  
   - `None` (مفيش تفاعل مهم)

في الآخر:

- بطلع:
  - `X_train`, `X_test`: بيانات التدريب والاختبار
  - `y_train`, `y_test`: التصنيفات
- وأحفظ:
  - ملفات `.npy` فيها الأرقام  
  - وملف `preprocessor.pkl` فيه:
    - تحويلات الأدوية لـ SMILES
    - الـ Label Encoder المسؤول عن تحويل الكلمات (Severe/Moderate/None) لأرقام

ممكن أقولها ببساطة:

> "الجزء ده بياخد بيانات خام عن الأدوية، ويحوّلها لأرقام جاهزة للنموذج يتعلم منها."

---

## 5. جزء تدريب النموذج (Model Training)

هنا بدرب نموذج ذكاء اصطناعي باستخدام **PyTorch** في ملف اسمه مثلاً: `model_training.py`.

اللي بيحصل:

1. أحمّل البيانات الجاهزة من خطوة المعالجة (`X_train`, `y_train`).
2. أبني نموذج اسمه **DeepDDI**:
   - ده نوع من الشبكات العصبية العميقة.
3. أدرّبه على أمثلة كتير:
   - كل مثال عبارة عن:
     - متجه أرقام يمثل دوائين
     - والتسمية: `Severe` أو `Moderate` أو `None`
4. بعد ما النموذج يتعلم:
   - أحفظ وزناته في ملف اسمه:  
     `deepddi_model.pt`
   - وأحفظ معلومات زي:
     - عدد المدخلات (input_dim)
     - عدد الكلاسات (num_classes)
     - أسماء الكلاسات نفسها

أقدر أشرح للجنة كده:

> "أنا درّبت نموذج ذكاء اصطناعي يتفرج على بيانات تفاعلات قديمة، ولما يفهم النمط، يقدر يجاوب على حالات جديدة ماشفهاش قبل كده."

---

## 6. جزء التنبؤ الفعلي (Prediction Module / API)

### 6.1 كلاس التنبؤ `DDIPredictor` (`predict.py`)

- عندي كلاس اسمه `DDIPredictor` بيعمل الآتي:
  1. يحمّل:
     - النموذج المتدرّب `deepddi_model.pt`
     - ملف `preprocessor.pkl`
     - ملف `model_info.json` اللي فيه معلومات عن النموذج
  2. لما أديله دوائين:
     - يجيب الـ SMILES بتاعهم
     - يحوّلهم لـ fingerprint رقمية
     - يدمجهم
     - يدخلهم النموذج
  3. يرجعلي:
     - الكلاس المتوقع (Severe / Moderate / None)
     - نسبة الاحتمال لكل كلاس
     - Risk score من 0 لـ 10
     - ورسالة عن مستوى الخطورة

ممكن أقول:

> "لما أدي للنظام اسم دوائين، الكلاس ده بيحوّلهم لأرقام، ويستخدم النموذج المتعلم عشان يقوللي نوع التفاعل ودرجة الخطورة."

### 6.2 جزء الـ API (ملف `api/main.py`)

- استخدمت **FastAPI** عشان أحوّل النموذج لـ **سيرفر**:
  - تطبيق خارجي (زي موبايل أب أو ويب) يبعث:
    - `drug_a`, `drug_b`
  - السيرفر يرد بتنبؤ فيه:
    - نوع التفاعل  
    - الخطورة  
    - تفاصيل إضافية

في نسخة تانية اسمها `main_with_firebase.py`:
- بتضيف:
  - **Firebase** لحفظ تاريخ التنبؤات
  - **Gemini AI** لشرح التفاعل للمستخدم بشكل نصي أوضح

---

## 7. مثال بسيط (لو في وقت تعرضه)

أشرح سيناريو:

> "لو عندي دواء (A) ودواء (B)، بدخلهم للنظام.  
> النظام يحول كل واحد لبصمة رقمية، يدمجهم، وبعدين النموذج يطلع مثلًا:
> - التفاعل: Severe  
> - Risk score: 8.5 من 10  
> ساعتها النظام ينبه إن الجمع بينهم خطر ولازم الطبيب ياخد باله."

لو عندك ديمو حقيقي من الـ API أو من برنامج بسيط، هنا الوقت المناسب تعرضه.

---

## 8. الخاتمة (30–45 ثانية)

ممكن يختم كده:

> "في النهاية، المشروع بتاعي هدفه يساعد في **تقليل الأخطاء الطبية** اللي بتحصل بسبب التفاعلات بين الأدوية.  
> استخدمت فيه:
> - معالجة بيانات طبية وكيميائية  
> - تمثيل كيميائي للأدوية (SMILES و Fingerprints)  
> - ونموذج ذكاء اصطناعي مبني بـ PyTorch  
> - مع واجهة API تقدر تتوصل مع تطبيق موبايل.  
>  
> إن شاء الله في المستقبل أقدر أزود عدد الأدوية المدعومة، وأحسّن دقة النموذج، وأضيف واجهة عربية مريحة للطبيب والمريض.  
> شكرًا لحضراتكم."

---

## 9. قسم أسئلة وأجوبة (Q&A) – ملخّص سريع

- **لو اتسأل: ليه الذكاء الاصطناعي؟**  
  > "لأن عدد التفاعلات كبير جدًا وصعب يتحكم فيه بقواعد ثابتة، الذكاء الاصطناعي يتعلم من بيانات حقيقية ويعمم على حالات جديدة."

- **لو اتسأل: البيانات منين؟**  
  > "من ملفات CSV فيها معلومات عن الأدوية والتفاعلات، وتم تجهيزها في مرحلة الـ Data Preprocessing."

- **لو اتسأل: فين ممكن نستخدم النظام؟**  
  > "في أنظمة المستشفيات، عيادات الأطباء، أو تطبيقات للصيادلة، عشان يتأكدوا قبل ما يجمعوا بين دوائين."

